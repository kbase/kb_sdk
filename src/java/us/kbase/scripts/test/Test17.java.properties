
import emptypackageparent.EmptyPackageParentClient;

import junit.framework.Assert;

public class Test17 {
	
	public Test17(EmptyPackageParentClient client) throws Exception {
	    String input = "1234";
		String output = client.getObject(input);
		Assert.assertEquals(input, output);
	}
}