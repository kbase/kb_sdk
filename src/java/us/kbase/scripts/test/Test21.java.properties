package us.kbase.test21;

import junit.framework.Assert;
import us.kbase.test21.dynamic.DynamicClient;

public class Test21 {
	
	public Test21(DynamicClient client) throws Exception {
	    String input = "1234";
		String output = client.getObject(input);
		Assert.assertEquals(input, output);
	}
}